# =====================================================
# Rss-Easy 环境变量配置（可选）
# =====================================================
#
# 💡 零配置启动：直接运行 docker-compose up -d 即可
#    - 密钥自动生成
#    - AI 配置可在应用界面中设置
#
# 只有以下场景需要修改此文件：
# 1. 修改端口或 URL（非 localhost 访问）
# 2. 提前配置 AI API Key
# =====================================================

# 应用 URL（非 localhost 访问时修改）
# NEXTAUTH_URL="http://your-domain.com"
# APP_URL="http://your-domain.com"

# Cookie 安全设置
# - HTTP 环境（如本地开发）：false
# - HTTPS 环境（生产部署）：true 或留空（自动检测）
# SECURE_COOKIE="false"

# =====================================================
# AI 服务配置（可选，也可在界面中配置）
# =====================================================

# AI_PROVIDER="openai"  # openai | anthropic | deepseek | custom
# AI_MODEL="gpt-4o"

# ---------- OpenAI ----------
# OPENAI_API_KEY="sk-xxx"

# ---------- Anthropic Claude ----------
# ANTHROPIC_API_KEY="sk-ant-xxx"

# ---------- DeepSeek ----------
# DEEPSEEK_API_KEY="sk-xxx"

# ---------- 自定义 API（OpenAI 兼容）----------
# CUSTOM_API_BASE_URL="https://api.moonshot.cn/v1"
# CUSTOM_API_KEY="sk-xxx"
# CUSTOM_API_MODEL="moonshot-v1-8k"
# AI_PROVIDER="custom"
